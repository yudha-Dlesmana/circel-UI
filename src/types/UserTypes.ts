type UserType = {
  username: string;
  email: string;
  name: string;
  bio: string;
  image: string;
};

type SuggestionType = {
  username: string;
  name: string;
  image: string;
};

export type { UserType, SuggestionType };
